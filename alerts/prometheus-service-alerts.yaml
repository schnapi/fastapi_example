groups:
  - name: global_health_alerts
    rules:
      - alert: ServiceDown
        expr: min_over_time(service_health[5m]) == 0
        labels:
          severity: critical
        annotations:
          summary: "Service {{ $labels.service }} is down"
          description: "Service {{ $labels.service }} has been unhealthy for the last 5 minutes."
